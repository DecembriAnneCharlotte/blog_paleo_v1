{{ form_start(form) }}

    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.photo) }}
    {{ form_row(form.date) }}
    {{ form_row(form.category) }}

    <div>
        <label for="{{ form.ingredients.vars.id }}">Ingredients:</label>
        {{ form_widget(form.ingredients, {'attr': {'multiple': 'multiple'}}) }}
    </div>

    <button type="submit" class="btn">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}